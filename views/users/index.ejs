<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explore Our Community</title>
    <link rel="stylesheet" href="/styles.css">
</head>

<body class="community-page">
    <%- include('../partials/_navbar.ejs') %>

        <main>
            <h1>Explore Our Community</h1>

            <% if (users.length> 0) { %>
                <ul class="community-list">
                    <% users.forEach((user)=> { %>
                        <li class="community-user">
                            <h2>
                                <%= user.username %>
                            </h2>
                            <a href="/users/<%= user._id %>/records" class="btn view-collection">View Their
                                Collection</a>
                        </li>
                        <% }); %>
                </ul>
                <% } else { %>
                    <p>No users to display yet.</p>
                    <% } %>
        </main>

        <% if (user) { %>
            <footer class="community-footer">
                <a href="/users/<%= user._id %>/records" class="btn back-to-collection-btn">Back to Your Collection</a>
            </footer>
            <% } %>
</body>

</html>